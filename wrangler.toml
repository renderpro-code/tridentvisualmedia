# -------------------------------------------------------
# Trident Visual Media — Cloudflare Pages build config
# Place this file at the root of your repository
# -------------------------------------------------------

[build]
  command = "hugo --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.147.0"
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"
